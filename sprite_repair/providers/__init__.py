"""Provider abstraction layer for SpriteRepair AI backends.

Currently hosts the OpenCode Go / Console providers; NVIDIA, OpenRouter and
Ollama remain implemented inline in ai_align.py (kept for compatibility).
"""
